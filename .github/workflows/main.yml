name: Windows

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - '.github/**'

jobs:
  windows:
    name: Windows x64 Release
    runs-on: windows-latest

    env:
      UPLOAD_ARTIFACT: "true"
      ONLY_CACHE: "false"
      PREPARE_PATH: "Telegram/build/prepare/prepare.py"
      
    defaults:
      run:
        shell: cmd

    steps:
      - name: Prepare directories.
        run: |
          mkdir %userprofile%\TBuild\Libraries
          mklink /d %GITHUB_WORKSPACE%\TBuild %userprofile%\TBuild
          echo TBUILD=%GITHUB_WORKSPACE%\TBuild>>%GITHUB_ENV%

      - name: Get repository name.
        shell: bash
        run: echo "REPO_NAME=${GITHUB_REPOSITORY##*/}" >> $GITHUB_ENV

      - uses: ilammy/msvc-dev-cmd@v1.13.0
        name: MSVC Environment
        with:
          arch: x64

      - name: Clone repository.
        uses: actions/checkout@v4
        with:
          submodules: recursive
          path: ${{ env.TBUILD }}\${{ env.REPO_NAME }}

      - name: Set environment.
        shell: bash
        run: |
          echo "CACHE_KEY=$(sha256sum $TBUILD/$REPO_NAME/$PREPARE_PATH | awk '{ print $1 }')" >> $GITHUB_ENV
          git config --global user.email "you@example.com"
          git config --global user.name "builder"

      - name: Cache ThirdParty.
        uses: actions/cache@v4
        with:
          path: ${{ env.TBUILD }}\ThirdParty
          # 再次更新 Key 名（v2），确保清理掉之前报错的残留环境
          key: ${{ runner.OS }}-x64-third-party-v2-${{ env.CACHE_KEY }}
          restore-keys: ${{ runner.OS }}-x64-third-party-v2-

      - name: Cache Libraries.
        uses: actions/cache@v4
        with:
          path: ${{ env.TBUILD }}\Libraries
          key: ${{ runner.OS }}-x64-libs-v2-${{ env.CACHE_KEY }}
          restore-keys: ${{ runner.OS }}-x64-libs-v2-

      - name: Prepare Libraries.
        env:
          GYP_MSVS_OVERRIDE_PATH: 'C:\Program Files\Microsoft Visual Studio\2022\Enterprise\'
          GYP_MSVS_VERSION: 2022
        run: |
          cd %TBUILD%
          :: 移除无效的 skip-debug 参数
          call %REPO_NAME%\Telegram\build\prepare\win.bat silent

      - name: Free disk space (Post-Prepare).
        run: |
          cd %TBUILD%
          :: 彻底清理掉 Debug 相关的库文件以腾出空间给 Release 编译
          if exist Libraries\Debug rmdir /s /q Libraries\Debug
          del /S Libraries\*.pdb
          del /S Libraries\*.pch
          del /S Libraries\*.obj

      - name: Patch cmake
        shell: bash
        run: |
          cd "$TBUILD/$REPO_NAME/cmake"
          sed -i '/Bcrypt/a \    runtimeobject.lib\n\    windowsapp.lib' options_win.cmake

      - name: Build Telegram Desktop (Release)
        if: env.ONLY_CACHE == 'false'
        run: |
          cd %TBUILD%\%REPO_NAME%\Telegram
          
          :: 明确指定生成器和 Release 模式
          call configure.bat ^
          -D CMAKE_BUILD_TYPE=Release ^
          -D DESKTOP_APP_DISABLE_AUTOUPDATE=ON ^
          -D DESKTOP_APP_DISABLE_CRASH_REPORTS=OFF ^
          -D DESKTOP_APP_NO_PDB=ON

          cmake --build ..\out --config Release --parallel 2

      - name: Move artifact.
        if: env.UPLOAD_ARTIFACT == 'true'
        run: |
          set OUT_DIR=%TBUILD%\%REPO_NAME%\out\Release
          mkdir artifact
          if exist "%OUT_DIR%\*.exe" (
              copy "%OUT_DIR%\*.exe" artifact\
          ) else (
              echo "Error: No executable found in %OUT_DIR%"
              dir /s %TBUILD%\%REPO_NAME%\out
              exit 1
          )

      - name: Upload artifact.
        uses: actions/upload-artifact@v4
        with:
          name: Telegram_x64_Release
          path: artifact\
